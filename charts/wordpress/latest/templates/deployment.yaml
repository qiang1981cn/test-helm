apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      run: nginx
  template:
    metadata:
      labels:
        run: nginx
    spec:
      containers:
      - env:
        - name: test
          value: test
        - name: aaabbb
          value: {{ .Values.env.tensorflow-gpu2.resources.limits.nvidia.com/gpu }}
        image: nginx
        name: nginx
